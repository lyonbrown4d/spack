# https://taskfile.dev

version: '3'

vars:
  BIN_NAME: spack
  DIST_DIR: dist
  # 默认平台
  DEFAULT_GOOS: linux
  DEFAULT_GOARCH: amd64
  DOCKERHUB_REPO: daiyuang/{{.BIN_NAME}}
  CONTAINER_PLATFORM_ALL: linux/amd64,linux/arm64
  CONTAINER_PLATFORM: '{{default .CONTAINER_PLATFORM_ALL .PLATFORMS}}'
tasks:
  dev:
    desc: development
    cmds:
      - cd test && pnpm build
      - go run github.com/air-verse/air@v1.62.0 .

  test:
    cmds:
      - go test ./...

  clean:
    cmds:
      - rm -rf {{.DIST_DIR}}

  build:
    desc: 构建当前平台
    cmds:
      - go build -trimpath -ldflags="-s -w -X main.Version={{.GIT_COMMIT}}" -o {{.DIST_DIR}}/{{.BIN_NAME}} .
    vars:
      GIT_COMMIT:
        sh: git log -n 1 --format=%h

  build:all:
    desc: 构建所有平台
    deps: [ build:linux, build:darwin, build:windows ]

  build:linux:
    desc: 构建 Linux 版本
    deps: [ clean ]
    env:
      GOOS: linux
      GOARCH: amd64
    cmds:
      - mkdir -p {{.DIST_DIR}}/linux
      - go build -trimpath -ldflags="-s -w" -o {{.DIST_DIR}}/linux/{{.BIN_NAME}} .

  build:darwin:
    desc: 构建 macOS 版本
    env:
      GOOS: darwin
      GOARCH: amd64
    cmds:
      - mkdir -p {{.DIST_DIR}}/darwin
      - go build -trimpath -ldflags="-s -w" -o {{.DIST_DIR}}/darwin/{{.BIN_NAME}} .
    deps: [ clean ]


  build:windows:
    desc: 构建 Windows 版本
    deps: [ clean ]
    env:
      GOOS: windows
      GOARCH: amd64
    cmds:
      - mkdir -p {{.DIST_DIR}}/windows
      - go build -trimpath -ldflags="-s -w" -o {{.DIST_DIR}}/windows/{{.BIN_NAME}}.exe .

  build:docker:alpine:
    desc: 构建 Alpine 运行时镜像
    cmds:
      - |
        docker buildx build \
          --target alpine \
          --platform {{.CONTAINER_PLATFORM}} \
          -t {{.DOCKERHUB_REPO}}:alpine \
          -t {{.DOCKERHUB_REPO}}:latest \
          -t {{.DOCKERHUB_REPO}}:{{.RELEASE_VERSION}} \
          -t {{.DOCKERHUB_REPO}}:alpine-{{.RELEASE_VERSION}} \
          -t {{.DOCKERHUB_REPO}}:alpine-{{.GIT_COMMIT}} \
          --push \
          .
    vars:
      GIT_COMMIT:
        sh: git log -n 1 --format=%h
      AUTO_RELEASE_VERSION:
        sh: go run ./scripts/release latest-no-v
      RELEASE_VERSION: '{{default .AUTO_RELEASE_VERSION .VERSION}}'

  build:docker:debian:
    desc: 构建 Debian Slim 运行时镜像
    cmds:
      - |
        docker buildx build \
          --target debian \
          --platform {{.CONTAINER_PLATFORM}} \
          -t {{.DOCKERHUB_REPO}}:debian \
          -t {{.DOCKERHUB_REPO}}:debian-{{.RELEASE_VERSION}} \
          -t {{.DOCKERHUB_REPO}}:debian-{{.GIT_COMMIT}} \
          --push \
          .
    vars:
      GIT_COMMIT:
        sh: git log -n 1 --format=%h
      AUTO_RELEASE_VERSION:
        sh: go run ./scripts/release latest-no-v
      RELEASE_VERSION: '{{default .AUTO_RELEASE_VERSION .VERSION}}'

  build:docker:
    desc: build container
    deps: [ build:docker:alpine,build:docker:debian ]

  check:
    cmds:
      - go run honnef.co/go/tools/cmd/staticcheck@v0.7.0 ./...

  lint:
    cmds:
      - go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.11.4 run ./...

  perf:bench:
    desc: 运行关键路径性能基线 benchmark
    cmds:
      - go test -run=TestDoesNotExist -bench='.' -benchmem -count=3 ./internal/resolver ./internal/assetcache ./internal/pipeline ./internal/server

  perf:profile:resolver:
    desc: 生成 resolver 基线 CPU/内存 profile
    cmds:
      - mkdir -p tmp/perf
      - go test -run=TestDoesNotExist -bench='BenchmarkResolverResolve' -benchmem -count=1 -cpuprofile tmp/perf/resolver.cpu.pprof -memprofile tmp/perf/resolver.mem.pprof ./internal/resolver

  perf:profile:cache:
    desc: 生成 asset cache 基线 CPU/内存 profile
    cmds:
      - mkdir -p tmp/perf
      - go test -run=TestDoesNotExist -bench='BenchmarkCacheGetOrLoad' -benchmem -count=1 -cpuprofile tmp/perf/cache.cpu.pprof -memprofile tmp/perf/cache.mem.pprof ./internal/assetcache

  perf:profile:pipeline:
    desc: 生成 pipeline enqueue 基线 CPU/内存 profile
    cmds:
      - mkdir -p tmp/perf
      - go test -run=TestDoesNotExist -bench='BenchmarkServiceEnqueue' -benchmem -count=1 -cpuprofile tmp/perf/pipeline.cpu.pprof -memprofile tmp/perf/pipeline.mem.pprof ./internal/pipeline

  perf:profile:http:
    desc: 生成 HTTP 资产路由基线 CPU/内存 profile
    cmds:
      - mkdir -p tmp/perf
      - go test -run=TestDoesNotExist -bench='BenchmarkHTTPAssetRoute' -benchmem -count=1 -cpuprofile tmp/perf/http.cpu.pprof -memprofile tmp/perf/http.mem.pprof ./internal/server

  release:tag:
    desc: "创建注释标签（用法: task release:tag VERSION=0.1.0）"
    requires:
      vars: [ VERSION ]
    vars:
      TAG: '{{if hasPrefix .VERSION "v"}}{{.VERSION}}{{else}}v{{.VERSION}}{{end}}'
    cmds:
      - git tag -a {{.TAG}} -m "release {{.TAG}}"

  release:goreleaser:check:
    desc: "校验 .goreleaser.yml 配置"
    cmds:
      - go run github.com/goreleaser/goreleaser/v2@latest check

  release:goreleaser:snapshot:
    desc: "使用 GoReleaser 执行本地快照构建（不发布）"
    cmds:
      - go run github.com/goreleaser/goreleaser/v2@latest release --snapshot --clean --skip=publish

  release:
    desc: "创建并推送标签（用法: task release VERSION=0.1.0）"
    requires:
      vars: [ VERSION ]
    vars:
      TAG: '{{if hasPrefix .VERSION "v"}}{{.VERSION}}{{else}}v{{.VERSION}}{{end}}'
    cmds:
      - task: release:goreleaser:check
      - task: release:tag
        vars:
          VERSION: '{{.VERSION}}'
      - git push origin {{.TAG}}

  release:bump:major:
    desc: "自动递增 major 并发布（用法: task release:bump:major）"
    vars:
      VERSION:
        sh: go run ./scripts/release bump-major
    cmds:
      - task: release
        vars:
          VERSION: '{{.VERSION}}'

  release:bump:minor:
    desc: "自动递增 minor 并发布（用法: task release:bump:minor）"
    vars:
      VERSION:
        sh: go run ./scripts/release bump-minor
    cmds:
      - task: release
        vars:
          VERSION: '{{.VERSION}}'

  release:bump:patch:
    desc: "自动递增 patch 并发布（用法: task release:bump:patch）"
    vars:
      VERSION:
        sh: go run ./scripts/release bump-patch
    cmds:
      - task: release
        vars:
          VERSION: '{{.VERSION}}'
