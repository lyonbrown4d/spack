name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go 1.26
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install task runner
        run: go install github.com/go-task/task/v3/cmd/task@v3.45.4

      - name: Run lint
        run: task lint

      - name: Run check
        run: task check

      - name: Run tests
        run: task test

      - name: Run race tests
        if: runner.os == 'Linux'
        run: task test:race

      - name: Run govulncheck
        if: runner.os == 'Linux'
        run: task security:govulncheck

      - name: Run build
        run: task build

      - name: Run container smoke test
        if: runner.os == 'Linux'
        run: task integration:container:smoke
